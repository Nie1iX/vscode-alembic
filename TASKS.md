## Планы работ по UI и конфигу Alembic

Ниже — детальный, пронумерованный список задач. Для каждой: краткое описание, UI, изменения в коде, крайние случаи, критерии приёмки, оценка и зависимости. Ссылки на файлы указываются в формате `path/to/file`.

1. ✅ Поддержка sqlalchemy.url в редакторе alembic.ini — **ВЫПОЛНЕНО**
   1.1. Описание: добавить поле для ввода `sqlalchemy.url` с валидацией DSN и маскированием пароля.
   1.2. UI: `src/webviews/settingsEditor.ts` — инпут + кнопка «Test connection» (необяз., через простой import драйвера не делаем, только формат и ping опционально позже).
   1.3. Код: обновление `[alembic].sqlalchemy.url` через `updateIniSection` из `src/utils/iniEditor.ts`.
   1.4. Края: пустое значение, переменные окружения (`${DB_URL}` или ini-стиль), спец-символы в пароле.
   1.5. Приёмка: URL сохраняется в `alembic.ini`, корректно читается обратно, не ломаются комментарии.
   1.6. Оценка: S-M. Зависимости: нет.

2. ✅ version_path_separator и recursive_version_locations — **ВЫПОЛНЕНО**
   2.1. Описание: управление мульти‑директориями версий.
   2.2. UI: селектор `version_path_separator` (= os/кастомный), чекбокс `recursive_version_locations`, редактор списка `version_locations` (мульти‑value с add/remove).
   2.3. Код: парс/сериализация списка путей; `validatePaths` проверяет каждую локацию; обновить помощь в `settingsEditor`.
   2.4. Края: абсолютные/отн. пути, несуществующие директории (предложить создать), разные разделители на Win/Unix.
   2.5. Приёмка: корректная запись ключей и валидация; создаёт папки по запросу.
   2.6. Оценка: M. Зависимости: 1.

3. ✅ revision_environment toggle — **ВЫПОЛНЕНО**
   3.1. Описание: включение загрузки `env.py` при `revision` (нужно для кастомных хук‑типов).
   3.2. UI: чекбокс в секции alembic.ini.
   3.3. Код: `[alembic].revision_environment = true/false`.
   3.4. Края: отсутствующий `env.py` → предупреждение.
   3.5. Приёмка: ключ сохраняется/читается, предупреждение при отсутствии `env.py`.
   3.6. Оценка: S. Зависимости: нет.

4. prepend_sys_path
   4.1. Описание: список путей, добавляемых в `sys.path`.
   4.2. UI: список с add/remove; кнопка проверки существования.
   4.3. Код: `[alembic].prepend_sys_path` (поддержать несколько значений через `os.pathsep`).
   4.4. Края: несуществующие пути, переменные окружения.
   4.5. Приёмка: записывается и пересобирается корректно, валидация путей работает.
   4.6. Оценка: S. Зависимости: 2.

5. Прочие базовые ini‑ключи: sourceless, output_encoding
   5.1. Описание: чекбокс `sourceless`, селектор `output_encoding` (utf-8 по умолчанию).
   5.2. UI: простой блок в редакторе ini.
   5.3. Код: обновление ключей в `[alembic]`.
   5.4. Края: неверная кодировка → предупреждение.
   5.5. Приёмка: корректная запись/чтение.
   5.6. Оценка: S. Зависимости: нет.

6. Post‑write hooks (black/ruff/autopep8/isort/zimports/pre-commit/custom)
   6.1. Описание: визуальный конфиг `[post_write_hooks]`.
   6.2. UI: мультиселект hooks; для каждого — `type`, `entrypoint`, `options`, `cwd`; пресеты; кнопка «Проверить наличие».
   6.3. Код: запись секции `[post_write_hooks]`; не ломать другие секции.
   6.4. Края: несколько хуков, порядок; отсутствие инструмента — мягкое предупреждение.
   6.5. Приёмка: корректная сериализация/десериализация, пресеты создают рабочую конфигурацию.
   6.6. Оценка: M-L. Зависимости: 1.

7. Логирование в ini (logging/handlers/formatters)
   7.1. Описание: настраивать уровни `logger_alembic`, `logger_sqlalchemy`, console/file handler.
   7.2. UI: селект уровня (ERROR/WARN/INFO/DEBUG), чекбокс «Лог в файл», путь к файлу.
   7.3. Код: создавать/обновлять секции `[logging]`, `[logger_*]`, `[handler_*]`, `[formatter_generic]`.
   7.4. Края: конфликт с уже кастомной конфигурацией — только минимально дотрагиваться (idempotent обновления).
   7.5. Приёмка: логи идут в консоль/файл согласно настройке, ini сохраняется без потери комментариев.
   7.6. Оценка: M. Зависимости: нет.

8. context.configure — include_schemas, include_object/include_name
   8.1. Описание: добавить toggles и шаблонные фильтры (by schema/table/glob/regex) в `env.py`.
   8.2. UI: форма фильтров; предпросмотр: «какие таблицы попадут» (best effort, по подключению опционально позже).
   8.3. Код: безопасный патч `env.py`: внедрение функций `include_object`/`include_name` и параметров `include_schemas=True`.
   8.4. Края: нестандартный `env.py` — бэкап и dry‑run diff; возможность отменить.
   8.5. Приёмка: `revision --autogenerate` учитывает фильтры; патч обратим.
   8.6. Оценка: M-L. Зависимости: 3.

9. context.configure — compare_type, compare_server_default, render_as_batch, version_table(+_schema)
   9.1. Описание: toggles для тонкой настройки автогенерации и имени таблицы версий.
   9.2. UI: чекбоксы + два текстовых поля.
   9.3. Код: патч `env.py` (добавление опций в `context.configure`).
   9.4. Края: SQLite требует `render_as_batch`; коллизии с существующими параметрами.
   9.5. Приёмка: опции применяются, автогенерация меняет поведение ожидаемо.
   9.6. Оценка: M. Зависимости: 8.

10. context.configure — sqlalchemy_module_prefix / user_module_prefix
   10.1. Описание: управление префиксами для типов.
   10.2. UI: два поля строковых значений.
   10.3. Код: патч `env.py` с добавлением параметров.
   10.4. Края: пустые значения → использовать дефолты.
   10.5. Приёмка: генерируемые ревизии используют указанные префиксы.
   10.6. Оценка: S. Зависимости: 8.

11. process_revision_directives пресеты (skip empty, направить в ветку)
   11.1. Описание: готовые варианты функции‑хука в `env.py`.
   11.2. UI: переключатель «Не создавать пустые ревизии», поле «head/branch для ревизий».
   11.3. Код: инъекция функции и передача в `context.configure(process_revision_directives=...)`.
   11.4. Края: совместимость с уже существующим хуком — объединение или предупреждение.
   11.5. Приёмка: при отсутствии изменений файл ревизии не создаётся; head устанавливается.
   11.6. Оценка: M. Зависимости: 8.

12. Multi‑DB поддержка (online/offline), отдельные SQL‑файлы
   12.1. Описание: список движков (name→URL/env var); оффлайн генерация в отдельные файлы.
   12.2. UI: таблица источников; кнопки «Generate SQL (offline)», «Run online».
   12.3. Код: шаблонные функции из доков Alembic (offline/online loops), параметр `output_buffer` для offline.
   12.4. Края: разные драйверы; отсутствие подключения — оффлайн доступен.
   12.5. Приёмка: генерируются N отдельных SQL; онлайн режим прогоняет по списку.
   12.6. Оценка: L. Зависимости: 8, 9.

13. UI формы команды revision
   13.1. Описание: собрать все флаги: `--autogenerate`, `--rev-id` (в т.ч. последовательный), `--head`, `--splice`, `--branch-label`, `--depends-on`, `--version-path`.
   13.2. UI: модалка/панель; выбор head из графа (в т.ч. `<label>@head`).
   13.3. Код: `src/services/alembicService.ts` — построение аргументов; поддержка `sequentialRevId*` настроек.
   13.4. Края: multiple heads без head → подсказка.
   13.5. Приёмка: правильный CLI вызов, файл создан в нужной директории, метаданные ревизии корректны.
   13.6. Оценка: M. Зависимости: 2, 15, 17.

14. UI формы upgrade/downgrade (включая offline SQL + -x)
   14.1. Описание: таргет `head/heads/<branch>@head/<hash>/±N`; оффлайн `--sql` с предпросмотром и сохранением.
   14.2. UI: селектор цели + переключатель `--sql`; редактор `-x key=value` (список пар), «Сохранить SQL».
   14.3. Код: формирование аргументов; Webview предпросмотра SQL.
   14.4. Края: multiple heads → подсказка про `<branch>@head` или `heads`.
   14.5. Приёмка: онлайн — выполняется; оффлайн — правильный SQL файл/превью.
   14.6. Оценка: M. Зависимости: 12 (для offline UX), 22.

15. UI merge
   15.1. Описание: выбор нескольких heads для merge; сообщение.
   15.2. UI: мультиселект из графа; поле message; опция `--depends-on` (доп. зависимости).
   15.3. Код: вызов `alembic merge`; обновление дерева/графа после.
   15.4. Края: неполные id; конфликт путей версий.
   15.5. Приёмка: создаётся merge‑ревизия с tuple `down_revision`; граф обновлён.
   15.6. Оценка: M. Зависимости: 13.

16. UI stamp
   16.1. Описание: выставить версию без миграции.
   16.2. UI: ввод/выбор ревизии/ветки.
   16.3. Код: `alembic stamp <target>`; обновить current.
   16.4. Края: multiple heads.
   16.5. Приёмка: `alembic current` отражает выставленную версию.
   16.6. Оценка: S. Зависимости: нет.

17. History/Show/Heads/Branches (+фильтры и диапазоны)
   17.1. Описание: UX для команд просмотра: фильтр по label, диапазоны `-r`, verbose.
   17.2. UI: панель с фильтрами; кликабельные элементы для `show`.
   17.3. Код: парс выводов, отрисовка; копирование команды в буфер.
   17.4. Края: большие истории — пагинация/ленивая подгрузка.
   17.5. Приёмка: корректные списки и переход к деталям.
   17.6. Оценка: M. Зависимости: нет.

18. Поддержка pyproject.toml ([tool.alembic])
   18.1. Описание: альтернатива `alembic.ini`; возможность переключить `alembic.configFile`.
   18.2. UI: переключатель «Использовать pyproject.toml»; редактор соответствующих секций.
   18.3. Код: парс TOML, маппинг ключей; миграция ini↔toml (мастер‑диалог).
   18.4. Края: частичные пересечения ключей; аккуратная синхронизация.
   18.5. Приёмка: корректное чтение/запись; команды читают новый конфиг.
   18.6. Оценка: L. Зависимости: 1-7.

19. Редактор метаданных ревизий (branch_labels, depends_on)
   19.1. Описание: редактирование шапки существующих файлов ревизий.
   19.2. UI: форма с мульти‑label и списком depends_on.
   19.3. Код: безопасное AST/регекс‑редактирование верхних констант; обновление `script.py.mako` для добавления `branch_labels` по умолчанию.
   19.4. Края: нестандартные шаблоны; бэкап перед правкой.
   19.5. Приёмка: изменения корректны, Импорты/код не повреждены.
   19.6. Оценка: M. Зависимости: 13, 15.

20. Async‑режим (env.py)
   20.1. Описание: мастер‑переключатель для `AsyncEngine`; подсказка по DSN `postgresql+asyncpg://`.
   20.2. UI: toggle «Использовать AsyncEngine» + справка.
   20.3. Код: патч `env.py` на async‑вариант (по докам), с откатом.
   20.4. Края: проект смешанный sync/async; индикация несовместимости драйвера.
   20.5. Приёмка: миграции работают в async; rollback на sync корректен.
   20.6. Оценка: M-L. Зависимости: 8, 12.

21. Диагностика окружения и конфликтов
   21.1. Описание: проверка `env.py`, `script_location`, `version_locations`, Python, наличие инструментов хуков.
   21.2. UI: кнопка «Validate environment», сводка OK/NOT FOUND.
   21.3. Код: расширить `validatePaths`; обнаружение multiple heads и быстрые действия (open merge UI).
   21.4. Края: монорепо; несколько `alembic.ini`.
   21.5. Приёмка: информативные алерты; кликабельные фиксы.
   21.6. Оценка: M. Зависимости: 2, 6.

22. Управление `-x key=value` (env tokens)
   22.1. Описание: глобальный список токенов по проекту; подстановка в команды.
   22.2. UI: редактор пар; чекбокс «использовать по умолчанию».
   22.3. Код: `ConfigurationManager` хранит map (новые ключи настроек), сервис Alembic добавляет к вызовам.
   22.4. Края: коллизии с руками переданными `-x`.
   22.5. Приёмка: токены применяются ко всем релевантным командам.
   22.6. Оценка: S-M. Зависимости: 14.

23. Доп. пресеты file_template (epoch, ISO8601) + превью имени
   23.1. Описание: новые шаблоны; превью итогового имени по введённому message.
   23.2. UI: выпадающий список + «Preview».
   23.3. Код: генерировать пример на клиенте; сохранить `file_template` в ini.
   23.4. Края: локаль/таймзона.
   23.5. Приёмка: шаблон сохраняется, превью соответствует.
   23.6. Оценка: S. Зависимости: 1.

24. Улучшение slug UX (truncate_slug_length)
   24.1. Описание: счётчик/ограничение символов для slug.
   24.2. UI: поле числа + live‑счётчик при наборе message в «Create Migration».
   24.3. Код: обрезать slug визуально; реальную логику оставляет Alembic.
   24.4. Края: non‑ASCII.
   24.5. Приёмка: UX показывает актуальную длину; ini‑ключ пишется.
   24.6. Оценка: S. Зависимости: 13.

25. Тест‑кейсы и документация
   25.1. Описание: обновить `TESTING_GUIDE.md` новыми сценариями; добавить unit для `iniEditor`, e2e smoke.
   25.2. UI: нет.
   25.3. Код: тесты на парс/сериализацию ini, безопасные патчи `env.py`.
   25.4. Края: комментарии и порядок ключей должны сохраняться.
   25.5. Приёмка: тест‑гайд покрывает все пункты; unit тесты зелёные.
   25.6. Оценка: M. Зависимости: 1-24.
